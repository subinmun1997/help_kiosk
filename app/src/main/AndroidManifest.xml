<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Help_kiosk"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>
        <activity android:name=".Start_kiosk" />
        <activity android:name=".foodList.chicken" />
        <activity android:name=".foodList.coffeeList.coffee" />
        <activity android:name=".foodList.hamberger" />
        <activity android:name=".foodList.kimbab" />
        <activity android:name=".foodList.salad" />
        <activity android:name=".foodList.soup" />
        <activity android:name=".foodList.coffeeList.bbacks.bbacks" />
        <activity android:name=".foodList.coffeeList.coffeebean.coffeebean" />
        <activity android:name=".foodList.coffeeList.ediya.ediya" />
        <activity android:name=".foodList.coffeeList.mega.mega" />
        <activity android:name=".foodList.coffeeList.compose.compose" />
        <activity android:name=".foodList.coffeeList.pascucci.pascucci" />
        <activity android:name=".foodList.coffeeList.hollys.hollys" />
        <activity android:name=".foodList.coffeeList.starbucks.starbucks" />
        <activity android:name=".foodList.coffeeList.twosome.twosome" />
    </application>

</manifest>